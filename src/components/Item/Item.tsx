import React from 'react';
import { Item } from './ItemType';

export function ItemComponent({
    id,
    title,
    description,
    link,
    feedUrl,
    pubdate,
    date,
    comments,
}: Item) {
    return (
        <div>
            
        </div>
    )
}
